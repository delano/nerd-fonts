# Inline Typographic Provenance

Nerd Fonts can patch a font so that text carries its own provenance: whether a
run of characters was written by a human, generated by an AI, or is of unknown
origin. The state travels in the text itself, as Unicode code points, and a
provenance-aware font renders variants of its own glyphs for those code points.
This document describes the implementation as it exists in this repository: the
encodings, the allocated code points, the `font-patcher --provenance` option,
the helper tools, and the measured behaviour. It is written for contributors
working on this feature and for authors of editors and other tools that want to
produce or consume provenance-marked text.

The protocol itself (states, encodings, fallback behaviour, and editor semantics)
is defined in the design document "Inline Typographic Provenance for Nerd
Fonts". This README covers only how it is wired into this repository.

## Behaviour

A provenance-aware patched font renders marked text with variants derived from
the source font's own glyphs. Unlike the icon sets in `src/glyphs/`, nothing is
copied from a separate symbol font: each variant is a reference to the base
glyph in the same font, so it keeps the base outline, advance width, and
vertical metrics.

| Encoding               | Text                        | Font with provenance glyphs | Font without provenance glyphs |
| ---------------------- | --------------------------- | --------------------------- | ------------------------------ |
| Variation selector     | `<base>` followed by `VS_*` | Variant glyph               | Plain base glyph               |
| Private Use Area (PUA) | `PUA_AI_<base>`             | `.ai` glyph                 | Missing-glyph display          |

Three states are generated: explicit human, AI, and unknown. Two further states
are reserved in the registry but not generated by the patcher (see below).
Unmarked text is treated as assumed human; there is no code point for it.

## Files

In this directory:

- `mapping.json`: the versioned registry of allocated code points.
- `README.md`: this document.

Related scripts:

- `bin/scripts/nfprov.py` (listed as `[4]` in `bin/scripts/README.md`):
  standard-library-only reference encoder and decoder for marked text.
- `bin/scripts/test-provenance.py` (listed as `[9]`): `fontTools`-based CI
  validator for patched fonts.
- `bin/scripts/generate-provenance-example.sh` (listed as `[4]`): patches a
  font in one or more styles, marks sample text, renders it with `hb-view`, and
  runs the validator. The quickest way to see the feature working end to end.

`bin/scripts/archive-font-patcher.sh` already sweeps all of `src/glyphs/` into
the archive, so `mapping.json` ships inside `FontPatcher.zip` with no change to
that script. If the file is missing or malformed at patch time, `font-patcher`
logs a critical error naming the path and exits before touching any glyph.

## Code-point allocation

### Variation selectors

| State           | Selector           | Status                  |
| --------------- | ------------------ | ----------------------- |
| Explicit human  | `U+E0100` (`VS17`) | Generated               |
| AI              | `U+E0101` (`VS18`) | Generated               |
| Unknown         | `U+E0102` (`VS19`) | Generated               |
| Human-edited AI | `U+E0103` (`VS20`) | Reserved; not generated |
| Mixed or other  | `U+E0104` (`VS21`) | Reserved; not generated |

These selectors are a private convention shared by encoders, decoders, and
patched fonts. They are not registered Unicode variation sequences.

### PUA counterparts

Provenance glyphs are not allocated in Supplementary PUA-A
(`U+F0000`–`U+FFFFD`): the `Material` patch set occupies `U+F0001`–`U+F1AF0`
there (`font-patcher`, `setup_patch_set`). Supplementary PUA-B (plane 16) is
used instead, with a fixed allocation:

```text
PUA_AI(cp) = 0x100000 + cp
```

This reserves a PUA counterpart for every Basic Multilingual Plane base
character without a lookup formula. It cannot overlap any glyph set this project
patches in, because every icon set lives in the BMP PUA or in Plane 15.

Profile `"version": 1` in `mapping.json` covers `U+0021`–`U+00FF` minus the
general categories `Zs`, `Cc`, and `Cf`: 188 entries, with no space, no
no-break space, no soft hyphen, and no control characters. The formula reserves
the rest of the range; later profiles must not give those code points a
different meaning.

### Stability rules

- Published `mapping.json` code points are never reassigned or removed.
- A profile that adds base characters or states increments `version` and
  appends entries only.
- Before changing an allocation range, check every `SymStart` and `SymEnd` in
  `font-patcher` and the [Codepoint Conflicts wiki page][wiki-conflicts].

### `mapping.json` format

```json
{
  "version": 1,
  "variation_selectors": {
    "human": "U+E0100",
    "ai": "U+E0101",
    "unknown": "U+E0102",
    "edited": "U+E0103",
    "mixed": "U+E0104"
  },
  "pua": {
    "U+100041": { "base": "U+0041", "provenance": "ai" }
  }
}
```

Although the PUA formula is fixed, consumers must read the `pua` table rather
than compute it. A future profile may restrict an entry or attach additional
metadata.

## `font-patcher` usage

```text
--provenance[={identical|subtle|explicit}]
```

The option lives in the `Symbol Fonts` argument group and uses the same
optional-value pattern as `--braille`. Given bare, it selects `identical`. It
is not enabled by `--complete`, so it is always opt-in, and a build without
`--provenance` is unchanged from upstream: the reference builds used in CI have
no format 14 `cmap` subtable and no `NFProv` tag. For the Symbols Only font it
logs a warning and does nothing, because variants need base glyphs from the
source font. Users on the fontconfig fallback route (`10-nerd-font-symbols.conf`)
therefore get no provenance glyphs.

Per base character, `add_provenance_glyphs()` skips the character when:

- the base code point is not in the font,
- the PUA code point is already occupied (`createChar()` would return that
  existing glyph and clobber its outline, `altuni`, and width),
- the base glyph already carries one of our selectors in its `altuni`, or
- a glyph named `<base>.ai`, `<base>.human`, or `<base>.unknown` already exists.

These skips are unconditional and are logged at debug level; `--careful` does
not affect them. Because of the second and third rules, re-patching an already
provenance-patched font adds nothing, and when nothing is added the encoding is
not rebuilt and the Version name is not tagged.

### Presentation styles

| Style       | `.human` and `.unknown` | `.ai`                                            |
| ----------- | ----------------------- | ------------------------------------------------ |
| `identical` | Reference to base glyph | Reference to base glyph                          |
| `subtle`    | Reference to base glyph | Base glyph plus a small dot below the baseline   |
| `explicit`  | Reference to base glyph | Base glyph plus a bar spanning the advance width |

`create_provenance_mark()` builds a single unencoded glyph named
`provenance.mark`, lazily and at most once per font, with `glyphPen` and
straight line segments only (`curveTo()` takes cubic or quadratic control
points depending on the source outline type, so Bezier segments would not be
portable). The `subtle` dot is an octagon of radius `em/24` (a diameter of
about half a typical period, chosen so it survives at terminal sizes), which
`derive_provenance_glyph()` translates to `width/2` so it sits centred under
the cell below the baseline. The `explicit` bar is `em/48` thick, drawn
canonically from `0` to `em` and scaled per glyph onto the base advance width.
Both are clamped into the space between the baseline and `font_dim['ymin']`,
with a margin of 12 % of that depth at each end, so the font's own descender
never has to be lowered. If there is no usable room below the baseline, a
warning is logged and the unmarked variants are used instead.

Glyph counts follow from this: `identical` adds 3 glyphs per eligible base
character, and the marked styles add 3 per base plus the one `provenance.mark`
glyph.

### Glyph naming and encoding

Variants are named `<base>.ai`, `<base>.human`, and `<base>.unknown`. All three
get an `altuni` entry pairing the base code point with the state's selector,
which is what makes FontForge emit a format 14 `cmap` subtable on export. The
`.ai` variant is additionally encoded at its PUA code point; `.human` and
`.unknown` are created with `createChar(-1, name)` and stay unencoded, so they
are reachable only through a selector. After the loop the encoding is rebuilt
as `UnicodeFull`. Existing base glyphs are never modified; their own kerning and
features are untouched.

### Patch order

`patch()` calls `add_provenance_glyphs()` after the patch-set loop and before
the glyph-count check and the `grave` fix-up. That order matters:
`set_sourcefont_glyph_widths()` has already normalised widths for `--mono`, so
the base width a variant copies is correct, and `copy_glyphs()` has finished,
so it cannot rebuild the encoding or clear `altuni` afterwards. Provenance is
deliberately not a `patch_set` entry: patch-set entries load a separate font
and scale and align its glyphs, while provenance variants reference glyphs
already in `self.sourceFont` and must keep their original scale.

When variants were added, the count is registered as
`glyphnum.update({"Provenance": (None, added)})`, so
`--experimental check-glyph-count` accounts for them. `check_glyph_counts()`
skips entries whose `ish` value is `None`, so no icon cheat-sheet file is
needed.

### Version metadata

`tag_provenance_version()` runs only when at least one variant was added. It
inserts `;NFProv 1` (from `projectNameAbbreviation` and the `version` field of
`mapping.json`, read at patch time rather than hard-coded)
immediately before the `Nerd Fonts x.y.z` segment of the Version name (name ID
5). The Version name is used because it is the only field that survives into
every consumer: `font.comment` and `font.fontlog` land in FontForge's private
`PfEd` table and are invisible to other tools. For example:

```text
Version 3.003;NFProv 1;Nerd Fonts 3.5.1
```

The position matters: `FontnameParser.rename_font()` derives the `UniqueID`
from the last whitespace-separated token of the Version name, so inserting
before the Nerd Fonts segment leaves the `UniqueID` unaffected. If the expected
segment is not found, the tag is appended and a warning is logged.

## Helper tools

### `bin/scripts/nfprov.py`

Python 3 standard library only. It loads `mapping.json` relative to its own
location (overridable with `--mapping`) and offers:

```text
nfprov.py inspect FILE
nfprov.py mark --human|--unknown|--ai [--mode=vs|pua] FILE
nfprov.py convert --from=vs|pua --to=vs|pua FILE
nfprov.py strip FILE
nfprov.py --selftest
```

`FILE` may be `-` for stdin; output goes to stdout unless `-o`/`--output` is
given. `inspect` prints a `key: value` report of per-state counts plus any
unrecognised selectors and PUA code points. `strip` warns on stderr that it is
lossy before writing. `--mode=pua` is only valid with `--ai`, since there is no
PUA encoding for the other states. `--selftest` runs the internal round-trip
tests used by CI.

Cluster rules for `mark`:

- The selector is placed after the whole cluster: base character plus combining
  marks (`Mn`/`Mc`/`Me`), VS15/VS16, skin-tone modifiers, ZWJ joins and what
  they join, and the second half of a regional-indicator pair.
- Whitespace is left unmarked.
- Marking is idempotent: an already-marked cluster, a bare selector, and a PUA
  provenance character are passed through untouched.
- In `--mode=pua`, only single-code-point bases present in `mapping.json` become
  PUA characters; multi-code-point clusters and bases outside the table fall
  back to the variation-selector encoding.

Input and output are UTF-8 regardless of locale, and newlines are read and
written verbatim, so CRLF line endings survive a round trip.

### `bin/scripts/test-provenance.py`

Needs `fontTools`; it is a CI tool only and is not a runtime dependency of
`font-patcher`. Given a patched font it checks that:

- a format 14 `cmap` subtable exists,
- every mapped PUA code point resolves to a glyph,
- `base + VS_AI` resolves to the same glyph as the PUA code point,
- `base + VS_HUMAN` and `base + VS_UNKNOWN` each resolve to a dedicated variant
  glyph rather than to the default glyph,
- each variant has the same advance width as its base glyph.

With `--reference REFERENCE_FONT` (the same font built without `--provenance`)
it also compares `head.yMin`, `head.yMax`, `hhea.ascent`, `hhea.descent`, and
the `OS/2` typo and win ascender and descender fields. Without `--reference`
that comparison is reported as skipped. `--selftest` builds a small synthetic
font in a temporary directory and validates it.

## CI

`.github/workflows/font-patcher.yml` runs, after the existing patch steps:

1. `nfprov.py --selftest` and `test-provenance.py --selftest`.
2. A Hack `--complete --provenance=subtle` build with
   `--experimental check-glyph-count`, validated by `test-provenance.py` with
   the plain Hack build as `--reference`.
3. A Hack `--complete --mono --provenance=explicit` build, validated the same
   way against the plain mono build.

The mono build is there because width normalisation precedes variant
generation.

### Release workflow: open decision

Release builds do not enable provenance. `gotta-patch-em-all-font-patcher!.sh`
forwards `NERDFONTS` to its `font-patcher` calls, so setting
`NERDFONTS: "--provenance=subtle"` in the release workflow would turn it on for
distributed fonts. That is deliberately not done yet: the project first has to
decide whether publicly distributed provenance-aware fonts need a distinct
family name, which affects `projectName` and `projectNameAbbreviation` in
`font-patcher`. This decision is open.

## Validating a change

For a quick end-to-end check, run the example script from anywhere:

```bash
bin/scripts/generate-provenance-example.sh identical subtle explicit
```

It writes patched fonts and one PNG per style to `temp/provenance-example/`,
which is ignored by git. When more than one style is given and ImageMagick is
installed, the PNGs are also stacked into `all-styles.png`. The manual steps below cover the same ground in more
detail. Use a scratch output directory and do not commit generated fonts.

```bash
out=$(mktemp -d)

fontforge --script ./font-patcher src/unpatched-fonts/Hack/Hack-Regular.ttf \
  --complete --quiet --no-progressbars --outputdir "$out"
fontforge --script ./font-patcher src/unpatched-fonts/Hack/Hack-Regular.ttf \
  --complete --provenance=subtle --debug 2 --outputdir "$out/prov"

python3 bin/scripts/test-provenance.py \
  --reference "$out/HackNerdFont-Regular.ttf" \
  "$out/prov/HackNerdFont-Regular.ttf"

# Both inputs must shape to A.ai.
hb-shape "$out/prov/HackNerdFont-Regular.ttf" -u "0041,E0101"
hb-shape "$out/prov/HackNerdFont-Regular.ttf" -u "100041"

# Ligature behaviour, on a font that has calt ligatures.
fontforge --script ./font-patcher src/unpatched-fonts/FiraCode/FiraCode-Regular.ttf \
  --complete --provenance=subtle --debug 2 --outputdir "$out/prov"
hb-shape "$out/prov/FiraCodeNerdFont-Regular.ttf" --features=calt \
  -u "003D,E0101,003E,E0101"

python3 bin/scripts/nfprov.py --selftest
python3 bin/scripts/test-provenance.py --selftest
```

Repeat for `--mono`, `--variable-width-glyphs`, and an OTF source.

## Observed behaviour

Measured with FontForge 20251009 and HarfBuzz.

- **Ligatures are lost.** On FiraCode, `calt` ligatures do not form whenever
  any member of the sequence carries a provenance selector; PUA code points
  inherit no lookups at all. The mechanism is a glyph-ID mismatch, not an
  intervening mark: the format 14 `cmap` resolves `<base, VS>` to the variant
  glyph before GSUB runs, so the source font's lookups see `equal.ai
  greater.ai` where they expect `equal greater`, and no selector glyph remains
  in the stream. Setting `IgnoreMarks` on the source lookups would therefore
  change nothing. There is no partial degradation. Kerning is likewise not
  inherited by marked or PUA text, for the same reason. A font-side fix is
  possible in principle (an early `ccmp` split of each variant back into the
  base glyph plus a zero-advance marker glyph that the source lookups skip) but
  is not implemented; see issue #3.
- **Fallback is asymmetric.** In a font without provenance glyphs, `base + VS`
  renders as the base glyph followed by a zero-advance selector, while a PUA
  code point renders `.notdef`. The variation-selector encoding is therefore
  the safe one for interchange; the PUA encoding is display-only.
- **References.** CFF output flattens the references (verified on the FiraMono
  OTF); TrueType output keeps them as 1- or 2-component composites (Hack).
- **Metrics.** The `head`, `hhea`, and `OS/2` tables are byte-identical to a
  build of the same font without `--provenance`.
- **Cost.** About +1 s on a roughly 37 s Hack `--complete` build, and about
  4.5 % more glyphs.
- **Reserved selectors.** `U+E0103` and `U+E0104` have no variants in the font
  and fall through to the base glyph.

### Verified renderers

Checked with the `subtle` Hack build from `generate-provenance-example.sh`,
loaded through `@font-face` from a local HTTP server, on macOS.

| Renderer                          | Result                                                  |
| --------------------------------- | ------------------------------------------------------- |
| HarfBuzz (`hb-view`, `hb-shape`)  | Correct: variants selected, marks rendered              |
| Chrome, Vivaldi                   | Correct                                                 |
| Firefox                           | Correct                                                 |
| Safari, Orion                     | No visual difference: base glyphs rendered, no marks    |
| Terminal emulators, editors       | Not yet tested                                          |

Safari and Orion both shape through WebKit and CoreText, and neither honoured
the format 14 subtable for these private selectors. The text still renders
cleanly (no boxes, no extra spacing), so the failure is silent. Other CoreText
hosts such as Terminal.app and iTerm2 are expected to behave the same way; that
is the next thing to verify. Whether CoreText can be made to honour the
selectors, for example by registering the sequences differently in `cmap`, is
an open question.

## Files intentionally unchanged

- `glyphnames.json`, `bin/scripts/lib/i_*.sh`, and `css/` describe icon glyphs.
  Provenance variants are source-font characters; `mapping.json` is their
  registry.
- The icon-set table in `src/glyphs/README.md` has no suitable upstream or
  license row for this feature, so that file gained only a one-line pointer to
  this directory.
- `Dockerfile`: no new runtime dependency.
- `fonts.json`, the release matrix, family names, and archive layout.

[wiki-conflicts]: https://github.com/ryanoasis/nerd-fonts/wiki/Codepoint-Conflicts
